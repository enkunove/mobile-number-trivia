name: number_trivia
description: "A new Flutter project."

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  get_it:
  flutter_bloc:
  equatable:
  dartz:
  data_connection_checker_nulls:
  dio:
  shared_preferences:

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail:

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

